--- Makefile.am.orig	2024-08-10 00:00:00
+++ Makefile.am	2024-08-10 00:00:00
@@ -145,7 +145,7 @@
 if BUILD_TOOLS
 	for tool in insmod lsmod rmmod depmod modprobe modinfo; do \
 		$(MKDIR_P) $(DESTDIR)$(sbindir); \
-		$(LN_S) --force --relative $(DESTDIR)$(bindir)/kmod $(DESTDIR)$(sbindir)/$$tool; \
+		$(LN_S) -srf $(DESTDIR)$(bindir)/kmod $(DESTDIR)$(sbindir)/$$tool; \
 	done
 endif
 
