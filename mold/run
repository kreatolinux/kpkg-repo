#!/bin/sh
NAME="mold"
VERSION="1.6.0"
SOURCES="https://github.com/rui314/mold/archive/refs/tags/v$VERSION.tar.gz"
SHA256SUM="59cd3ea1a2a5fb50d0d97faddd8bff4c7e71054a576c00a87b17f56ecbd88729  mold-1.6.0.tar.gz"
DESCRIPTION="A Modern Linker"

build() {
    tar -xvf v$VERSION.tar.gz
    cd mold-$VERSION || exit 1
    mkdir build
    cd build
    cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=c++ -DCMAKE_INSTALL_PREFIX=/usr ..
    cmake --build . -j $(nproc)
    cmake --install .
}
