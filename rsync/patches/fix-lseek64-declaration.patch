--- a/syscall.c
+++ b/syscall.c
@@ -391,8 +391,6 @@ OFF_T do_lseek(int fd, OFF_T offset, int whence)
 {
 #ifdef HAVE_LSEEK64
 #if !defined(__LP64__) && SIZEOF_OFF64_T == 8
-	off64_t lseek64();
-
 	return lseek64(fd, offset, whence);
 #endif
 #endif
